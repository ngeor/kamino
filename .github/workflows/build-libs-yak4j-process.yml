name: Build yak4j-process

on:
  push:
    branches: [ master ]
    paths:
      - libs/yak4j-process/**
      - .github/workflows/build-libs-yak4j-process.yml
      - pom.xml
  pull_request:
    branches: [ master ]
    paths:
      - libs/yak4j-process/**
      - .github/workflows/build-libs-yak4j-process.yml
      - pom.xml

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Set up JDK 11
      uses: actions/setup-java@v2
      with:
        java-version: '11'
        distribution: 'zulu'
        cache: maven
    - name: Build with Maven
      run: mvn -B -ntp clean verify --file libs/yak4j-process/pom.xml
