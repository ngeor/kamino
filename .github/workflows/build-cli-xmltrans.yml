name: Build xmltrans

on:
  push:
    branches: [ master ]
    paths:
      - cli/xmltrans/**
      - .github/workflows/build-cli-xmltrans.yml
  pull_request:
    branches: [ master ]
    paths:
      - cli/xmltrans/**
      - .github/workflows/build-cli-xmltrans.yml

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Set up JDK 11
      uses: actions/setup-java@v2
      with:
        java-version: '11'
        distribution: 'zulu'
        cache: maven
    - name: Build with Maven
      run: mvn -B -ntp verify --file cli/xmltrans/pom.xml
