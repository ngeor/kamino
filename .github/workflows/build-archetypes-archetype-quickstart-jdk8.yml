name: Build archetype-quickstart-jdk8

on:
  push:
    branches: [ master ]
    paths:
      - archetypes/archetype-quickstart-jdk8/**
      - .github/workflows/build-archetypes-archetype-quickstart-jdk8.yml
  pull_request:
    branches: [ master ]
    paths:
      - archetypes/archetype-quickstart-jdk8/**
      - .github/workflows/build-archetypes-archetype-quickstart-jdk8.yml

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Set up JDK 11
      uses: actions/setup-java@v2
      with:
        java-version: '11'
        distribution: 'zulu'
        cache: maven
    - name: Build with Maven
      run: mvn -B -ntp verify --file archetypes/archetype-quickstart-jdk8/pom.xml
