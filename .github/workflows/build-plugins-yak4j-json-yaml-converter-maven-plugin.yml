name: Build yak4j-json-yaml-converter-maven-plugin

on:
  push:
    branches: [ master ]
    paths:
      - plugins/yak4j-json-yaml-converter-maven-plugin/**
      - .github/workflows/build-plugins-yak4j-json-yaml-converter-maven-plugin.yml
  pull_request:
    branches: [ master ]
    paths:
      - plugins/yak4j-json-yaml-converter-maven-plugin/**
      - .github/workflows/build-plugins-yak4j-json-yaml-converter-maven-plugin.yml

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Set up JDK 11
      uses: actions/setup-java@v2
      with:
        java-version: '11'
        distribution: 'zulu'
        cache: maven
    - name: Build with Maven
      run: mvn -B -ntp verify --file plugins/yak4j-json-yaml-converter-maven-plugin/pom.xml
